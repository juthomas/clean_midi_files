Synthetic fixtures used by tests.

- `corrupted.mid`: intentionally invalid bytes to verify batch resilience.
- Valid MIDI fixtures are generated programmatically in tests to keep the repository lightweight.

